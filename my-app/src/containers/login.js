import React from 'react';

function login(){
    return 
    (
        <div>
            <h1>User Profile</h1>
        </div>
    );

}

export default login;
// default for if we want to export the whole login page
